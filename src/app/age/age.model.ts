export class Age {
  day: number;
  month: number;
  year: number;
 constructor (day:number, month:number, year:number) {
    this.day = day;
    this.month = month;
    this.year = year;
 }
 // constructor (public day:any, public month:any, public year:any) {
 // }
}
